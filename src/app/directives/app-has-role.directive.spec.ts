import { AppHasRole } from './app-has-role.directive';

describe('AppHasRole', () => {
  it('should create an instance', () => {
   // const directive = new AppHasRole();
    //expect(directive).toBeTruthy();
  });
});
